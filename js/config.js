/**
 * Настройки общего рейтинга (Supabase).
 *
 * Пока поля пустые, игра работает как раньше — только с локальной таблицей
 * рекордов. Заполните оба значения из панели Supabase:
 *   Project Settings → Data API → Project URL и публичный ключ anon.
 *
 * Ключ anon предназначен для публикации в коде клиента: доступ ограничивается
 * не его секретностью, а политиками Row Level Security на стороне базы.
 * Инструкция по настройке таблицы — в README, раздел «Общий рейтинг».
 */
const SUPABASE = {
  url: '',      // например: https://abcdefghijklm.supabase.co
  anonKey: ''   // длинная строка, начинается с eyJ...
};
